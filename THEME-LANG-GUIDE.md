# 🎨 دليل الوضع الداكن والتبديل بين اللغات

## ✅ تم إضافة المميزات الجديدة

### 🌙 الوضع الداكن (Dark Mode)
- زر في الـ Navbar
- يحفظ الاختيار في localStorage
- انتقال سلس بين الأوضاع
- يعمل على جميع الصفحات

### 🌐 التبديل بين اللغات
- زر AR/EN في الـ Navbar
- يحفظ اللغة المختارة
- يغير اتجاه النص (RTL/LTR)
- يترجم المحتوى تلقائياً

---

## 🎯 كيفية الاستخدام

### للمستخدم:
1. اضغط على أيقونة القمر 🌙 للوضع الداكن
2. اضغط على AR/EN للتبديل بين اللغات
3. الاختيارات تُحفظ تلقائياً

---

## 🔧 كيفية التعديل

### 1️⃣ تعديل ألوان الوضع الداكن

في `style.css` ابحث عن `[data-theme="dark"]`:

```css
[data-theme="dark"] {
    --bg-light: #1a1a1a;        /* ← لون الخلفية الرئيسية */
    --bg-white: #2d2d2d;        /* ← لون البطاقات */
    --text-primary: #f5f5f7;    /* ← لون النص الرئيسي */
    --text-secondary: #a1a1a6;  /* ← لون النص الثانوي */
}
```

**لتغيير الألوان:**
- غير القيم بالألوان التي تريدها
- استخدم أدوات مثل [Coolors.co](https://coolors.co) لاختيار الألوان

---

### 2️⃣ إضافة ترجمات جديدة

في `main.js` ابحث عن `translations`:

```javascript
const translations = {
    en: {
        nav: {
            home: 'Home',  // ← غير النص الإنجليزي
            about: 'About'
        }
    },
    ar: {
        nav: {
            home: 'الرئيسية',  // ← غير النص العربي
            about: 'من أنا'
        }
    }
};
```

**لإضافة صفحة جديدة:**
```javascript
en: {
    nav: {
        newPage: 'New Page'  // أضف هنا
    }
},
ar: {
    nav: {
        newPage: 'صفحة جديدة'  // أضف هنا
    }
}
```

---

### 3️⃣ إضافة الأزرار لصفحات أخرى

انسخ هذا الكود في أي صفحة HTML:

```html
<div class="navbar-controls">
    <button class="theme-toggle" id="themeToggle" onclick="toggleTheme()" title="Toggle Dark Mode">
        <i class='bx bx-moon'></i>
    </button>
    <button class="lang-toggle" id="langToggle" onclick="toggleLanguage()" title="Switch Language">
        <span class="lang-text">AR</span>
    </button>
</div>
```

**ضعه داخل `<nav class="navbar">` بعد `<ul class="nav-links">`**

---

## 🎨 تخصيص الأزرار

### تغيير حجم الأزرار:
```css
.theme-toggle,
.lang-toggle {
    width: 40px;   /* ← غير العرض */
    height: 40px;  /* ← غير الارتفاع */
    font-size: 20px; /* ← غير حجم الأيقونة */
}
```

### تغيير الألوان:
```css
.theme-toggle:hover {
    background: #ff3b30;  /* ← لون مختلف عند التمرير */
    color: white;
}
```

---

## 📱 Responsive

الأزرار متجاوبة تلقائياً:
- **Desktop**: تظهر بجانب القائمة
- **Mobile**: تظهر في الأعلى اليسار

---

## 🧪 اختبار

### اختبر الوضع الداكن:
1. اضغط على أيقونة القمر 🌙
2. تحقق من تغير الألوان
3. حدث الصفحة - يجب أن يبقى الوضع الداكن

### اختبر اللغات:
1. اضغط على AR/EN
2. تحقق من تغير النصوص
3. تحقق من اتجاه النص (RTL/LTR)
4. حدث الصفحة - يجب أن تبقى اللغة

---

## 🎯 ملاحظات مهمة

### ✅ الوضع الداكن:
- يعمل على جميع الصفحات
- يحفظ الاختيار
- انتقال سلس
- يؤثر على جميع العناصر

### ✅ التبديل بين اللغات:
- يعمل على الصفحة الرئيسية
- يحفظ الاختيار
- يغير الاتجاه (RTL/LTR)
- يترجم النصوص الأساسية

### 📝 لترجمة صفحات أخرى:
أضف الترجمات في `translations` ثم حدث دالة `updatePageContent()`

---

## 🚀 الخلاصة

**تم إضافة:**
- ✅ زر الوضع الداكن 🌙
- ✅ زر التبديل بين اللغات 🌐
- ✅ حفظ الاختيارات
- ✅ تصميم متجاوب
- ✅ انتقالات سلسة

**الموقع الآن أكثر احترافية! 🎉**
